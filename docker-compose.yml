version: '3.8'

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin1
      POSTGRES_DB: DundlinMuffin
    ports:
      - "1112:5432"
    volumes:
      - ./dbdata:/var/lib/postgresql/data

